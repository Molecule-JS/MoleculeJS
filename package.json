{
  "devDependencies": {
    "@types/chai": "^4.1.3",
    "@types/mocha": "^5.2.1",
    "@types/puppeteer": "^1.3.3",
    "bundlesize": "^0.17.0",
    "chai": "^4.1.2",
    "chromedriver": "^2.40.0",
    "eslint": "^5.0.1",
    "eslint-plugin-html": "^4.0.3",
    "express": "^4.16.3",
    "gulp": "^3.9.1",
    "gulp-clean": "^0.4.0",
    "lerna": "^2.11.0",
    "mocha": "^5.2.0",
    "puppeteer": "^1.6.1",
    "rollup": "^0.63.0",
    "rollup-plugin-replace": "^2.0.0",
    "rollup-plugin-terser": "^1.0.1",
    "rollup-plugin-typescript2": "^0.16.0",
    "run-sequence": "^2.2.1",
    "ts-loader": "^4.4.1",
    "tslint": "^5.10.0",
    "tslint-config-airbnb": "^5.9.2",
    "typescript": "^3.0.0-dev.20180630",
    "vuepress": "^0.10.2"
  },
  "private": true,
  "scripts": {
    "test:modules": "npm run build && gulp rollup:test && env DEBUG=\"puppeteer:*\" node test/browser/puppeteer.js && npm run clean:test",
    "build:modules": "gulp rollup:dist:es",
    "build:modules:dev": "gulp rollup:dev:es",
    "build:iife:dev": "gulp rollup:dev:iife",
    "build:iife": "gulp rollup:dist:iife",
    "clean:test": "gulp clean:test",
    "clean:dist": "gulp clean:dist",
    "clean:cache": "gulp clean:cache",
    "clean": "npm run clean:dist && npm run clean:test && npm run clean:cache",
    "test": "npm run test:modules",
    "build": "npm run clean && lerna bootstrap && npm run build:modules && npm run build:iife && npm run build:dev && npm run clean:cache",
    "build:dev": "npm run build:modules:dev && npm run build:iife:dev && npm run clean:cache",
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs"
  }
}
